---
deployment:
  tasks:
    - export DEPLOYPATH=/home/crashbar/public_html/
    - /usr/bin/rsync -a --exclude='.git/' --exclude='.gitignore' --exclude='.cpanel.yml' --exclude='content.json' --exclude='README.md' --exclude='DEPLOY.md' --exclude='CLAUDE.md' ./ $DEPLOYPATH
